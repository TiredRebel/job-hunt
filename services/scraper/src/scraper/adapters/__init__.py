"""Source adapters implementing :class:`scraper.ports.SourceAdapter`.

One module per source; parsing is kept in pure module-level functions so
tests can run against recorded fixtures without network access
(see docs/SOURCES.md for per-source strategy and risk notes).
"""
