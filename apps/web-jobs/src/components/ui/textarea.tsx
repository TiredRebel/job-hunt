/**
 * @module components/ui/textarea
 *
 * TODO(web-micro-frontends cutover): remove this shim once remotes import
 * `@job-hunter/web-ui/textarea` directly and apps/web is retired.
 */
export * from '@job-hunter/web-ui/textarea';
