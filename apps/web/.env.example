# API base URL used by Server Components / route handlers (not exposed to the browser).
API_URL=http://localhost:4000/v1

# API base URL used by Client Components. Must be reachable from the browser
# (same value as API_URL in local dev, since both run on localhost).
NEXT_PUBLIC_API_URL=http://localhost:4000/v1
