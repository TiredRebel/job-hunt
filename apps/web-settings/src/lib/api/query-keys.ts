/**
 * @module lib/api/query-keys
 *
 * TODO(web-micro-frontends cutover): remove this shim once remotes import
 * `@job-hunter/web-api/query-keys` directly and apps/web is retired.
 */
export * from '@job-hunter/web-api/query-keys';
