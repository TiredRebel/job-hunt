/**
 * @module lib/utils
 *
 * TODO(web-micro-frontends cutover): remove this shim once remotes import
 * `@job-hunter/web-ui/utils` directly and apps/web is retired.
 */
export { cn } from '@job-hunter/web-ui/utils';
