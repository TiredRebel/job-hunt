/**
 * @module lib/api/types
 *
 * TODO(web-micro-frontends cutover): remove this shim once remotes import
 * `@job-hunter/web-api/types` directly and apps/web is retired.
 */
export * from '@job-hunter/web-api/types';
