# Tasks — Phase 7 Hardening

Scoped for implementation by **Sonnet 5**. Groups are ordered by the design's
migration plan; each group ends with a verification step and must keep every
existing gate green before the next group starts. Gate commands (run from each
service dir): scraper/llm `uv run pytest -q && uv run ruff check . && uv run ruff format --check . && uv run mypy --strict src`; api/web `npm run typecheck && npm run lint && npm run test && npm run build`.

## 1. Observability — Python services (scraper + llm)

- [x] 1.1 Add deps to `services/scraper/pyproject.toml` and `services/llm/pyproject.toml`: `python-json-logger`; then `uv sync` in both. (Also add `tenacity` to `services/llm` now to avoid a second sync — used in group 6.)
- [x] 1.2 Create `services/scraper/src/scraper/observability.py` with: (a) a `correlation_id: ContextVar[str | None]` and `get_correlation_id()`/`set_correlation_id()` helpers; (b) a `logging.Filter` subclass that injects the current correlation id onto each `LogRecord`; (c) a `configure_logging(level: str)` that installs a `python-json-logger` JSON formatter on the root + uvicorn loggers with the filter attached (single-line JSON, fields: timestamp, level, logger, message, correlation_id); (d) a Starlette-style ASGI middleware class `CorrelationIdMiddleware` that reads `X-Correlation-Id` (mint `uuid4().hex` if absent), sets the contextvar for the request, and adds `X-Correlation-Id` to the response headers. Fully type-annotated (`mypy --strict` clean).
- [x] 1.3 Add `log_level: str = "info"` to `services/scraper/src/scraper/config.py` `Settings` (env `LOG_LEVEL`).
- [x] 1.4 In `services/scraper/src/scraper/main.py`: call `configure_logging(get_settings().log_level)` at startup (lifespan or module init) and register `CorrelationIdMiddleware` on the FastAPI app. Confirm existing `logger = logging.getLogger(__name__)` lines now emit JSON with the id.
- [x] 1.5 Create `services/llm/src/llm/observability.py` mirroring 1.2 exactly (identical shape — the two files may be near-duplicates; that is acceptable, they are per-service). Add `log_level` to `services/llm/src/llm/config.py` and wire `configure_logging` + `CorrelationIdMiddleware` in `services/llm/src/llm/main.py` (mirror 1.3–1.4).
- [x] 1.6 Tests: in each service add a test asserting (a) an inbound `X-Correlation-Id` is echoed on the response and used in logs (capture via `caplog`/a JSON parse), and (b) a request with no header gets a minted id echoed back. Use each service's existing FastAPI test-client pattern.
- [x] 1.7 Verify: run both Python service gate commands. All green, including the two new tests.

## 2. Observability — gateway + web propagation

- [x] 2.1 Add `nestjs-cls` to `apps/api/package.json`; `npm install`.
- [x] 2.2 In `apps/api/src/infrastructure/logger/logger.module.ts`, set `pinoHttp.genReqId` to read the incoming `X-Correlation-Id` (case-insensitive) or mint a UUID, and set it on the response `X-Correlation-Id` header; keep the existing level/pretty behavior.
- [x] 2.3 Register `ClsModule.forRoot({ middleware: { mount: true, setup: (cls, req) => cls.set('correlationId', req.id) } })` (or an interceptor that copies pino's `req.id` into CLS) in `apps/api/src/app.module.ts`, so the request id is available to singleton services. (Used interceptor mode, not middleware — see logger.module.ts/app.module.ts comment: this is ordering-independent, since Nest interceptors always run after all Express-level middleware including pino-http's `req.id` assignment.)
- [x] 2.4 In each of `apps/api/src/infrastructure/clients/http-scraper.client.ts`, `http-llm-admin.client.ts`, `http-llm-cover-letter.client.ts`: inject `ClsService`, and in the shared `connection()`/header builder add `'X-Correlation-Id': cls.get('correlationId')` when present. (One small shared header helper is fine.)
- [x] 2.5 In `apps/web/src/app/api/[...path]/route.ts`: forward an incoming `X-Correlation-Id` from the browser request, or mint one (`crypto.randomUUID()`) when absent, on the outbound gateway `fetch`; also return it on the proxy response. Keep the existing `Content-Type`-only body/hop behavior otherwise.
- [x] 2.6 Test (`apps/api`): a vitest asserting an inbound correlation id reaches an outbound client header (mock `fetch`, drive one client through a CLS context seeded with a known id). This is the D2 correctness guard.
- [x] 2.7 Verify: run api + web gate commands. All green (104/104 api, 56/56 web, both builds clean). Manual curl round-trip against a live process hit Windows/WSL node-interop friction in this environment (native `npx tsx` resolves to the Windows-side node.exe, whose network binding doesn't cleanly reach `localhost` from this WSL shell) — deferred to the group-9 full Docker rebuild, which does this exact check.

## 3. Coverage gates — measure, scope, ratchet

- [x] 3.1 Add `@vitest/coverage-v8` to `apps/api` and `apps/web`; add `pytest-cov` to both Python services (already `uv sync`'d if grouped, else sync). (Installed `@vitest/coverage-v8@3` — matches the repo's pinned `vitest@^3.2.4`; latest `@4` requires `vitest@4` and conflicts.)
- [x] 3.2 `apps/api/vitest.config.ts`: enable `coverage` (provider `v8`). Actual layout differs from the assumed `src/domain/**`+`src/application/**` split — those two top-level dirs are pure type/port declarations with zero executable statements; the real business logic is each bounded context's `*.service.ts` (already ~98% covered). Scoped `include` to `src/**/*.service.ts` + `src/**/*.guard.ts` + `src/common/**/*.interceptor.ts` instead — controllers/DTOs/modules/infrastructure stay excluded, matching CODING_STANDARDS.md's "supertest for controllers" split (a separate, not-yet-added test layer). Measured: 98.14% stmts / 93.37% branch / 98.5% funcs / 98.14% lines.
- [x] 3.3 Set `coverage.thresholds` in `apps/api/vitest.config.ts` at 95/90/95/95 (measured 98.14/93.37/98.5/98.14, rounded down with margin) — verified the gate actually fails when unmet (temporarily set statements to 99, confirmed exit 1, reverted). `apps/web/vitest.config.ts` scoped to `src/lib/**` (the only tested layer — all components/pages/UI primitives are at 0%, a known pre-existing gap, not something this gate should paper over or fail on); measured 49.54/86.66/84.9/49.54, thresholds set to 49/85/80/49, gate-failure verified the same way.
- [x] 3.4 Python: added `[tool.coverage.run]`/`[tool.coverage.report]` + `addopts = "--cov=... --cov-report=term-missing"` to each `pyproject.toml`, scoping to domain/application: scraper excludes `main.py`/`db.py`/the three concrete IO-transport fetchers (httpx/crawl4ai/agent-browser); llm excludes `main.py`/`db.py`/`listener.py`/the four provider HTTP adapters. Measured: scraper 93.57%, llm 98.75% (both scoped). `fail_under` set to 90 (scraper) / 95 (llm). Found and fixed a real subtlety: `pytest-cov`'s two internal checks use different comparison semantics (rounded vs raw), so a `fail_under` value within rounding distance of the actual (e.g. 99 vs 98.75) prints "FAIL" but does **not** fail the exit code — verified both gates genuinely fail with an unambiguous gap (100) and pass at the real thresholds.
- [x] 3.5 Ensure the coverage flags run via the existing test entrypoints so CI picks them up unchanged (`npm run test` → vitest with `coverage.enabled: true`; `uv run pytest` → `addopts` includes `--cov`). Verified via bare `npm run test`/`uv run pytest` with no extra flags. CI job command shape unchanged (group 8 adds the e2e job). Also fixed two gaps coverage-enabled-by-default surfaced: `apps/web`'s bare `eslint` (unlike api's `eslint src`) was linting the generated `coverage/` report directory — added to `globalIgnores` in `eslint.config.mjs`; `.gitignore` had `coverage/` and `htmlcov/` but not coverage.py's `.coverage`/`.coverage.*` data files — added.
- [x] 3.6 Verify: all four services' test commands pass **with coverage enforcement on** at the newly-set thresholds — full sweep re-run (pytest+ruff+format+mypy ×2, typecheck+lint+test+build ×2), all green. Thresholds documented above; also recorded in PROGRESS.md's group-9 log entry.

## 4. API rate limiting

- [x] 4.1 Add `@nestjs/throttler` to `apps/api`; `npm install`. (Installed `^6.5.0`, compatible with Nest 11.)
- [x] 4.2 Add `RATE_LIMIT_TTL` (seconds/ms per the module's contract) and `RATE_LIMIT_LIMIT` to `apps/api/src/config/api-config.schema.ts` with sane defaults (e.g. 60s / 120 req) and to `ApiConfig`. (Defaults: `RATE_LIMIT_TTL=60000` ms, `RATE_LIMIT_LIMIT=120`.)
- [x] 4.3 Register `ThrottlerModule.forRootAsync` (reading the config) in `apps/api/src/app.module.ts` and provide `ThrottlerGuard` as an `APP_GUARD`. Configure the tracker to use the real client address (trust-proxy / `getTracker`) so the web `/api` proxy hop is not treated as one client. New `infrastructure/rate-limit/tracker.ts` (`resolveClientTracker`): prefers `X-Forwarded-For`'s leftmost entry, falls back to the socket address. **Known limitation discovered during implementation** (documented in the module's own header comment): this Next.js version's App Router Route Handlers have no documented client-IP accessor (checked the bundled `node_modules/next/dist/docs` per `apps/web/AGENTS.md`'s own instruction — no `request.ip`, nothing in the Route Handlers doc), and the web `/api` proxy doesn't yet forward `X-Forwarded-For`. So today, all browser traffic through that proxy is bucketed by the web container's own address — still real protection (any caller is bounded), just coarser for that one path than for a direct caller or a real reverse-proxy topology. Fixing the web-proxy side is a separate, scoped follow-up, not guessed at here.
- [x] 4.4 Annotate the automation controller `apps/api/src/automation/automation.controller.ts` (internal-token `/v1/automation`) with `@SkipThrottle()` so automation is never throttled.
- [x] 4.5 Test (`apps/api`): new `src/app.throttling.spec.ts` — a real ephemeral-port Nest app (no supertest dependency, plain `fetch`) proving (a) requests within the limit pass, (b) a public route hit past its configured limit returns 429, (c) a `@SkipThrottle()` route never throttles even well past the same limit. No fake timers needed (long `ttl`, few fast sequential requests).
- [x] 4.6 Verify: api gate command green — 107/107 tests (104 + 3 new), typecheck/lint/build clean.

## 5. Per-source politeness

- [x] 5.1 In `services/scraper/src/scraper/fetchers/gate.py`: change `acquire(url)` to accept optional per-call overrides `min_delay: float | None`, `jitter: float | None`, `respect_robots: bool | None`, falling back to the instance defaults. `_pause` uses the effective delay/jitter; robots consultation honors the effective `respect_robots`. The per-host lock, `_last_request`, and robots cache stay shared (design D7 — one budget per host). Overrides bundled as a new `PolitenessOverrides` frozen dataclass in `fetchers/base.py` (avoids a circular import with `gate.py`), taken as `acquire(url, *, overrides: PolitenessOverrides | None = None)`.
- [x] 5.2 Thread per-source politeness from `core.sources.config` through the call path. Extended the `PageFetcher` protocol itself with an optional `politeness: PolitenessOverrides | None = None` kwarg on `get()` (additive — existing callers unaffected), implemented by all three concrete transports (`httpx_fetcher.py`, `crawl4ai_fetcher.py`, `agent_browser.py`) and forwarded by `EscalatingFetcher`. New `SourceBoundFetcher` wrapper (in `fetchers/base.py`) binds one source's fixed overrides and is constructed fresh per source in `registry.create_adapter()` (new `_politeness_overrides_from_config()` reads `min_delay`/`jitter`/`respect_robots` from `config`, ignoring missing/wrong-typed keys rather than raising) — this means **adapters themselves need zero changes**: they keep calling `self._fetcher.get(url)` exactly as before, and the wrapper transparently supplies the bound overrides. Handles the `api`-strategy case (a shared singleton fetcher, not reconstructed per source) the same way as crawl4ai/agent-browser.
- [x] 5.3 Kept the service-global `Settings.min_delay_seconds`/`jitter_seconds`/`respect_robots` (already existed) as the defaults when a source sets nothing.
- [x] 5.4 Tests: `test_registry.py` (new) — source overrides reach the fetcher, no-override-keys falls back to all-`None` (gate defaults), wrong-typed keys ignored not raised. `test_gate.py`'s existing scenarios (shared gate, identical per-host decision) all still pass unmodified, proving the additive change didn't disturb them. Fixed 3 pre-existing fake `PageFetcher` test doubles (`test_escalating.py`, `test_api.py`, `conftest.py`'s shared `FakeFetcher`) to accept the new protocol parameter.
- [x] 5.5 Verify: scraper gate command green — 88/88 tests (85 + 3 new) passed, 1 skipped, coverage 93.78% ≥ 90%, ruff/format/`mypy --strict` clean.

## 6. Request resilience — retry with backoff

- [x] 6.1 Create `apps/api/src/infrastructure/clients/fetch-with-retry.ts`: an async `fetchWithRetry(url, init, opts)` doing bounded exponential backoff + jitter; retry only on network error, 5xx, or 429 (honor `Retry-After`); max attempts from config; on each retry emit a warning log (via the injected pino logger / CLS correlation id) naming target + attempt number; after the last attempt rethrow/return the final response unchanged. Fully typed.
- [x] 6.2 Add `DOWNSTREAM_RETRY_ATTEMPTS` (default 3) and optional base-delay to `api-config.schema.ts`/`ApiConfig`.
- [x] 6.3 Route the **safe** client calls through `fetchWithRetry`: `http-scraper.client.ts` `listUnprocessed`/`listAdapters`/`testSource` (GET + side-effect-free); LLM admin `listProviders`/`listModels` (GET) plus `testProvider` (POST, but the llm-admin-ui spec requires it never activate the provider or touch the resolver cache — genuinely side-effect-free, same reasoning as scraper's `testSource`). `triggerScrape` and `markProcessed` (scraper) and `createProvider`/`updateProvider`/`deleteProvider` (llm-admin) left single-attempt with an explanatory code comment each (run-creation, attempt-counter increment, resource creation, unreviewed mutation, and delete-then-404-on-retry respectively). `http-llm-cover-letter.generate` also left single-attempt (non-deterministic output, real LLM cost, records a pipeline run) — comment added citing design D6.
- [x] 6.4 LLM service: wrapped `post_json`/`get_json`/`probe` in `services/llm/src/llm/providers/base.py` (the shared helpers all three provider adapters call — zero changes needed to `ollama.py`/`openai_compat.py`/`anthropic.py` themselves) with `tenacity.AsyncRetrying` — `stop_after_attempt`, `wait_exponential_jitter`, a custom `retry_if_exception` predicate covering `httpx.RequestError` (network) and `httpx.HTTPStatusError` with status 429/5xx; attempts from new `Settings.provider_retry_attempts` (default 3); each retry logged at warning under the correlation id via `before_sleep`.
- [x] 6.5 Tests: (`apps/api`) new dedicated `fetch-with-retry.spec.ts` — 8 tests: first-try success, transient 503 retried to eventual success, 429 with `Retry-After` honored, non-transient 4xx not retried, bounded give-up returning the last response, network error retried then succeeds, network error rethrown after exhausting attempts, retry warning logged naming target + attempt number. (`services/llm`) new `test_provider_retry.py` — 4 tests: transient (503×2) then success is retried, non-transient (400) is not retried (single attempt), retries bounded by configured attempts, `probe()` reports failure after exhausting retries. Verified tenacity's `AsyncRetrying` async-iterator pattern and `reraise=True` behavior with standalone smoke tests before wiring into production code.
- [x] 6.6 Verify: api (115/115 tests, typecheck/lint/build clean) + llm (70/70 tests, coverage 98.76% ≥ 95%, ruff/format/mypy --strict clean) gate commands green.

## 7. Dead-letter — configurable limit + inspection endpoint

- [x] 7.1 Scraper: added `GET /jobs_raw/dead-letter` to `main.py` returning rows in `processing_status='failed'` (id, source_id/slug, external_id, url, title, process_attempts, processed_at) via new `db.list_dead_letter(limit)` + new `DeadLetterRow` TypedDict. No token/auth check added — the scraper service has **no** app-level auth on any route today (trust is network-level/Docker-internal, not token-based); matching that exactly rather than introducing an inconsistent one-off check.
- [x] 7.2 **Already satisfied by existing code** — `Settings.max_process_attempts` (env `SCRAPER_MAX_PROCESS_ATTEMPTS`, default 3) was already the configurable attempt limit used by `mark_processed`/the `POST /jobs_raw/{id}/mark` route; no new variable added (a second one would violate "one source of truth", which this task explicitly warns against).
- [x] 7.3 Gateway: added `DeadLetterJob` + `listDeadLetter()` to the scraper client port, implemented in `http-scraper.client.ts` (retried — GET, side-effect-free); `AutomationService.deadLetterJobs()`; `GET /v1/automation/jobs/dead-letter` on `automation.controller.ts` (new `DeadLetterJobsQueryDto`, bare-array response matching the existing `unnotifiedMatches` pattern rather than introducing a wrapper object) with new `DeadLetterJobResponse` DTO. OpenAPI regenerated (`npm run openapi:emit`) and `packages/shared-ts` client regenerated (`npm run generate`) — both confirmed to include the new path/schema.
- [x] 7.4 Tests: scraper `test_list_dead_letter_returns_only_failed_rows` (mixed pending/failed fixture rows → only the failed one returned, via `FakeDb`); gateway `AutomationService.deadLetterJobs` — 2 new tests (passthrough of the scraper client's listing, limit respected) against the in-memory `FakeScraperClient`.
- [x] 7.5 Verify: scraper (89/89 pytest +1 skipped, coverage 93.78% ≥ 90%, ruff/format/mypy clean) + api (117/117 tests, typecheck/lint/build clean) + shared-ts (typecheck/lint/build clean) all green.

## 8. E2e happy path in CI

- [x] 8.1 Added an `e2e` job to `.github/workflows/ci.yml`. **Design change from the plan**: does not use `infra/docker-compose.yml` — that compose file explicitly assumes Postgres already exists on the _host_ (`host.docker.internal:5432`, documented in its own header comment) and has no Postgres service of its own, so it's unsuitable for CI as-is. Also, `apps/web`'s own `playwright.config.ts` already boots the web app itself via `webServer: { command: 'npm run dev -- --port 3000' }` — a native process, not a container — so the job doesn't need to run `web` via Docker either. Landed on `docs/DEPLOYMENT.md` §8.2's own documented "native processes" approach instead: a GitHub Actions `postgres:17` service container, `dbmate up` + `infra/db/seed.sql` run directly via `psql` (not `npm run db:seed`, which is hardcoded to `docker exec pg-learn` — this dev's local container name, absent in CI), scraper/llm started natively via `uv run uvicorn` (the Windows/WSL `psycopg`/`ProactorEventLoop` block documented in DEPLOYMENT.md is Windows-specific and doesn't apply to the Linux `ubuntu-latest` runner), the gateway via `npm run dev -w apps/api`, bash curl-loop health waits for all three, then Playwright (`npx playwright install --with-deps chromium` — the bare npm script lacks `--with-deps`, needed for Chromium's OS libraries on a fresh runner). One real job seeded into `core.jobs` directly via SQL so the happy path exercises the full interactive flow rather than hitting its own empty-state skip. Playwright report uploaded on `always()`.
- [x] 8.2 The existing e2e spec (`apps/web/e2e/jobs-happy-path.spec.ts`) already has exactly this skip logic — skips the suite when the API is unreachable, and skips the row-interaction assertions when the job table is empty — no spec changes were needed. `continue-on-error: true` applied with an explicit `TODO(phase-7-hardening)` comment naming what "stable" means and why it's needed (this job's sequence — fresh Postgres, two natively-booted Python services, a native gateway — could not be verified against a real GitHub Actions runner from this environment).
- [x] 8.3 **Already satisfied by group 3** — no new CI YAML needed. `turbo.json`'s `test` task has no special config masking failures; `npm run check` (the `node` job) runs `turbo run lint typecheck test`, and both `apps/api`/`apps/web`'s `test` scripts now run vitest with `coverage.enabled: true` (fails the process on an unmet threshold, verified in group 3). Same for `scraper`/`llm` jobs' existing `uv run pytest -q` — `addopts` now includes `--cov`, no flag needed.
- [x] 8.4 Verify: `act`/`actionlint` unavailable in this environment; validated via `python3 -c "import yaml; yaml.safe_load(...)"` (genuine YAML syntax check, re-run after every edit) and careful manual trace against `DEPLOYMENT.md` §3.4/§8.2 and the live stack — empirically confirmed the gateway's real health path is `/v1/health` (not bare `/health`) by curling the running Docker containers, confirmed the seed SQL's `INSERT ... ON CONFLICT` logic against the live DB in a rolled-back transaction, confirmed `npm run <script> -w apps/api` resolves the workspace correctly via `npm pkg get`. Could not run the job end-to-end — no real GitHub Actions runner available here; this honest limitation is exactly why `continue-on-error` is in place.

## 9. Env, docs, and final gates

- [x] 9.1 Added every new env var to the relevant `.env.example` files: root (`SCRAPER_LOG_LEVEL`, `LLM_LOG_LEVEL`, `LLM_PROVIDER_RETRY_ATTEMPTS`), `apps/api/.env.example` (`RATE_LIMIT_TTL`, `RATE_LIMIT_LIMIT`, `DOWNSTREAM_RETRY_ATTEMPTS`). No `PROCESSING_MAX_ATTEMPTS`/generic `PROVIDER_RETRY_ATTEMPTS` added — the former already existed as `SCRAPER_MAX_PROCESS_ATTEMPTS` (group 7), the latter is `LLM_PROVIDER_RETRY_ATTEMPTS` via the service's existing prefix convention. `infra/docker-compose.yml` needed **no changes** — scraper/llm/api all use `env_file: ../.env`, which picks up new root-`.env` vars automatically; none of the new vars need a container-specific override.
- [x] 9.2 Updated `docs/ARCHITECTURE.md` §9 (correlation-id chain described end to end, `/metrics` explicitly marked deferred — it was previously listed as done, which was aspirational) and §10 (coverage scoping per package, e2e-in-CI now real); `docs/CODING_STANDARDS.md` (correlation-id header name, coverage command + scoping per language); `docs/DEPLOYMENT.md` (§5.1/§5.3/§5.4 new env vars + per-source-politeness note, §9 known-gaps entry for the unverified e2e job, §10.1 rewritten for the real CI job).
- [x] 9.3 Flipped all six Phase 7 checklist items in `PROGRESS.md` to `[x]` (header ◐ → ✅) and appended a comprehensive dated log entry covering every group's outcome, including the measured coverage thresholds and the e2e-job design pivot.
- [x] 9.4 Full-repo gate sweep: scraper (89/89 +1 skipped), llm (70/70), api (117/117), web (56/56), shared-ts — all typecheck/lint/format/mypy/build clean; root `npm run check` + `npm run build` (matching CI's `node` job exactly) both exit 0. Rebuilt all four Docker images and redeployed the full stack (`docker compose --profile services up -d --build`); smoke-checked `/health` on scraper/llm/gateway/web (200 on all four) and proved the correlation-id chain live: a direct gateway call with an incoming id echoes it back, a call with none mints a fresh UUID, and a call **through the web `/api` proxy** carries the id all the way to the gateway and back. Also went one step further than asked: proved an application-level log line (not uvicorn's own access logger, which correctly shows `correlation_id: null` since it runs outside any application middleware) carries the correct id, by triggering a real scrape run with a correlation id header and grepping the resulting JSON log line.
- [x] 9.5 Updated the wiki checkpoint (`wiki/pages/current-state.md` + `wiki/log.md`) per the repo's checkpoint convention.
