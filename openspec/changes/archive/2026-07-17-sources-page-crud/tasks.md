# Tasks: sources-page-crud

## 1. Scraper service — adapter list + test endpoint

- [x] 1.1 Add `probe()` to every adapter (implemented as an `async def probe(self) -> FetchResult` on the `SourceAdapter` protocol, rather than a plain `probe_url` string main.py would fetch itself — keeps "which URL, which fetcher" fully inside the adapter, no layering violation): dou/workua/jobua reuse `self._list_url`; Reddit fetches the first configured subreddit's `new.json`; Upwork fetches its RSS feed URL. Also added `status_code: int | None` to `FetchResult` (populated by `HttpxFetcher`, `None` for rendering transports) so the test endpoint can report a real HTTP status.
- [x] 1.2 Added `GET /adapters` to `services/scraper/src/scraper/main.py` returning `{"slugs": sorted(known_slugs())}`
- [x] 1.3 Added `POST /sources/{slug}/test` to `main.py`: looks up the source row (404 unknown slug in DB), resolves adapter (`no_adapter` on `UnknownSourceError`), resolves fetcher via the existing factory (`unsupported_strategy` on `UnsupportedStrategyError`), calls `adapter.probe()`; maps `FetchBlockedError` → `blocked`, `FetchUnavailableError`/any other exception → `failed`, success → `ok` with `http_status`/`elapsed_ms`; response model `SourceTestResponse{status, detail, http_status, elapsed_ms}` (snake_case — the gateway proxy maps to camelCase); no run row, no `jobs_raw` writes.
- [x] 1.4 Tests added in `services/scraper/tests/test_api.py`: adapters endpoint lists the 5 registered slugs; test endpoint per outcome (`ok`/`no_adapter`/`unsupported_strategy`/`blocked`/`failed` via both `FetchUnavailableError` and an unexpected exception); explicit no-side-effects test (`db.finished is False`, i.e. no run lifecycle touched). `FakeAdapter` in `test_runner.py` gained a stub `probe()` to keep satisfying the extended protocol.
- [x] 1.5 Gates green: 80/80 pytest (+1 skipped live-smoke, unrelated) + ruff check + ruff format + mypy --strict.

## 2. Gateway — CRUD + proxy endpoints

- [x] 2.1 Extended `SourceRepository` port + `postgres-source.repository.ts`: `create(input)` uses `INSERT ... ON CONFLICT (slug) DO NOTHING RETURNING *` → `null` on conflict (matches this repository's own existing null-for-miss convention, e.g. `setEnabled`, rather than introducing a thrown-error convention like the keyword-dictionaries module uses); `update(slug, patch)` builds a dynamic `SET` clause for only the provided fields (mirrors `postgres-keyword-dictionary.repository.ts`'s update method), returning `Source | null`.
- [x] 2.2 Extended `ScraperClient` port + `http-scraper.client.ts`: `listAdapters(): Promise<readonly string[]>` (`GET /adapters`) and `testSource(slug): Promise<SourceTestResult>` (`POST /sources/{slug}/test`, snake_case → camelCase mapping).
- [x] 2.3 DTOs in `sources.dto.ts`: `CreateSourceDto` (slug `@Matches(/^[a-z0-9-]+$/)`, name, baseUrl `@IsUrl`, fetchStrategy `@IsIn([...])`, config `@IsObject` optional, enabled optional) and `UpdateSourceDto` (same minus slug, all optional); `SourceTestResponse` + `AdapterListResponse` added to `sources.response.dto.ts` with `@ApiProperty`.
- [x] 2.4 `SourcesService`: `create` (ConflictException on duplicate slug), `update` (NotFoundException on miss), `test` (404 via existing `get()` for gateway-unknown slugs, then delegates to scraper client, wrapping any transport failure in BadGatewayException), `adapters` (thin proxy).
- [x] 2.5 `SourcesController`: `GET /sources/adapters` declared BEFORE `GET :slug` (verified by reading the file — Nest matches same-verb routes in declaration order), `POST /sources` (201 via Nest's POST default), `PATCH /sources/:slug`, `POST /sources/:slug/test` (`@HttpCode(200)` override, since Nest defaults POST to 201) — all with full Swagger annotations.
- [x] 2.6 Vitest specs added to `sources.service.spec.ts`: create success + ConflictException on duplicate, update success + NotFoundException on miss, test delegates to scraper + NotFoundException when gateway-unknown + BadGatewayException on scraper transport failure, adapters proxy. Deliberately skipped: a dedicated HTTP-level route-order test — this repo has no supertest/e2e-controller test harness yet (only direct-instantiation unit tests like `health.controller.spec.ts`), and adding one just for a single, well-documented Nest behavior (same-verb routes match in declaration order) was judged disproportionate; verified by code reading instead, matching the file's existing test-style ceiling. Also had to add `listAdapters`/`testSource` stubs to the _other_ `FakeScraperClient` in `automation.service.spec.ts` to keep it satisfying the extended interface.
- [x] 2.7 Gates green: 88/88 vitest (+8 new) + tsc + eslint + build.

## 3. Contract — OpenAPI + generated client

- [x] 3.1 Ran `npm run openapi:emit -w apps/api` then `npm run generate -w packages/shared-ts`; verified all 5 new/changed `/v1/sources*` paths and operation IDs (`SourcesController_create_v1`, `_update_v1`, `_adapters_v1`, `_test_v1`) landed in both `openapi.json` and `src/generated/api.ts`.
- [x] 3.2 `packages/shared-ts` gates green: typecheck + lint + build.

## 4. Web — Add / Edit / Test on the sources page

- [x] 4.1 Added to `lib/api/sources.ts`: `createSource`, `updateSource`, `testSource`, `listAdapters` typed against the regenerated `ApiOperations` (`CreateSourceBody`/`UpdateSourceBody`/`SourceTestResult` derived via `OperationBody`/`OperationResponse`, matching `lib/api/dictionaries.ts`'s convention); new `SourceFetchStrategy` type; new `queryKeys.sources.adapters` entry.
- [x] 4.2 New `components/sources/source-form-dialog.tsx`: shadcn `Dialog`, mode `create | edit`; fields slug (immutable in edit)/name/baseUrl/fetchStrategy (`Select`)/config (`Textarea` with JSON.parse inline validation, submit disabled on invalid)/enabled (`Switch`); 409 → inline slug error, other errors → toast; success invalidates `queryKeys.sources.all` and closes. Split into an outer `SourceFormDialog` (no state) + inner `SourceFormBody`, mounted only while `open` and keyed by `source?.slug ?? 'create'` — a fresh mount gives fresh `useState` for free; avoids a reset-on-prop-change effect entirely (this repo's ESLint enforces `react-hooks/set-state-in-effect` as an error, which an effect-based reset tripped).
- [x] 4.3 `sources-page.tsx`: "Add source" `Button` above and below the list (both open the dialog in create mode); per-row Edit icon (`Pencil`) opening edit mode; per-row Test icon (`Zap`) with in-flight pending text and persistent inline result rendering (`ok` default tint + elapsed ms; `no_adapter`/`unsupported_strategy`/`blocked`/`failed` in warning tint + the API's `detail` text).
- [x] 4.4 Adapter awareness: `useQuery` on `listAdapters` in `SourcesPageClient` (failure leaves `adapterSlugs` `undefined`, silently omitting badges — no separate error state for the page); "no adapter" `Badge` on rows whose slug is absent from the list; warning line inside the create/edit form when the typed slug has no adapter.
- [x] 4.5 i18n: added all new keys to `apps/web/messages/en.json` and `uk.json` (form labels, validation errors, test outcome labels, add-source button, no-adapter badge) — both validated as parseable JSON after editing.
- [x] 4.6 Vitest specs added in `lib/api/sources.spec.ts` (matching the established API-client test pattern from `cover-letters.spec.ts` — this repo has no component-rendering tests for _any_ admin page yet, so a new `sources-page`/`source-form-dialog` component-test harness was judged out of scope/inconsistent for this change): create success + 409 propagation, update success + 404 propagation, test success + 502 propagation, adapters list unwrapping.
- [x] 4.7 Gates green: 44/44 vitest (+7 new) + tsc + eslint (same 2 pre-existing unrelated warnings) + build.

## 5. Verification & docs

- [x] 5.1 Manual smoke verified against the running Docker stack (real browser automation, real network — not mocked): rebuilt all 4 images and redeployed; Test `dou` → `OK (424 ms): fetched https://jobs.dou.ua/vacancies/` (real live fetch through the politeness gate); Add source `djinni` (crawl4ai, `https://djinni.co`) → "Source created" toast, row appears with a "No adapter" badge and a live warning in the form while typing the slug; Test `djinni` → `No adapter: no adapter registered for source 'djinni'`; Edit `workua`'s base URL → "Source updated" toast, confirmed persisted via `GET /v1/sources/workua`; Test `upwork` → `Failed: Client error '410 Gone' for url 'https://www.upwork.com/ab/feed/jobs/rss'` — a genuine real-world failure (the legacy RSS feed is actually gone now), proving the `failed` path end to end. Note: the `djinni` test row has no delete endpoint (explicitly out of scope, see design.md non-goals) — it's left in the DB as real verification data; remove via SQL if a pristine seed state is wanted.
- [x] 5.2 `openspec/specs/sources-admin/spec.md` sync happens at archive time (delta already written, not touched here); `wiki/pages/current-state.md` rewritten as a fresh checkpoint, `wiki/log.md` appended; `PROGRESS.md` Phase 5 line extended + new log entry added.
- [x] 5.3 Full monorepo gates green: root `npm run check` (10/10 tasks) + `npm run build` (3/3); `services/scraper` 80/80 pytest + ruff + mypy; `services/llm` unaffected (not touched by this change). Caught and fixed one real bug during this pass: `CreateSourceDto.enabled`'s `@ApiPropertyOptional({ default: true })` made `openapi-typescript` generate `enabled` as **required** in the TS type (a documented quirk — that generator treats any property with a schema-level `default` as always-present, which is wrong for a request body). `apps/web`'s standalone `tsc --noEmit` caught it in the new `sources.spec.ts` test payloads; `next build`'s narrower type check had not. Removed `default: true` from the decorator, regenerated `openapi.json` + the shared-ts client — `enabled` is correctly `?:` now.
